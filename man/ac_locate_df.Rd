% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locate.R
\name{ac_locate_df}
\alias{ac_locate_df}
\title{Locate pattern matches as a data frame}
\usage{
ac_locate_df(
  ac,
  doc,
  ...,
  overlapping = FALSE,
  na = c("omit", "keep", "error")
)
}
\arguments{
\item{ac}{An \verb{<ac_automaton>} object created by \code{ac_build()}.}

\item{doc}{A character vector of documents to search.}

\item{...}{Must be empty. This is used to require optional arguments to be
supplied by name.}

\item{overlapping}{Default is \code{FALSE}. If \code{TRUE}, report overlapping
matches. This is only supported when \code{ac} was built with
\code{match_kind = "standard"}.}

\item{na}{How to handle \code{NA} documents. \code{"omit"} drops missing documents
(default); \code{"keep"} returns one row with missing result columns for each
missing document; \code{"error"} fails.}
}
\value{
A data frame with one row per match and four columns:
\code{doc_id}, \code{pattern_id}, \code{start}, and \code{end}.
}
\description{
\code{ac_locate_df()} is the data-frame form of \code{\link[=ac_locate]{ac_locate()}}. It is useful when
you want one row per match instead of one list element per document.
}
\examples{
ac <- ac_build(c("hello", "world"))
doc <- c("hello world", "nothing", "world hello")
ac_locate_df(ac, doc)
}
\seealso{
\code{\link[=ac_locate]{ac_locate()}}, \code{\link[=ac_locate_bytes]{ac_locate_bytes()}}, \code{\link[=ac_extract_df]{ac_extract_df()}}.
}
