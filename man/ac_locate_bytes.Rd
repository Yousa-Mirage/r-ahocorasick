% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locate.R
\name{ac_locate_bytes}
\alias{ac_locate_bytes}
\title{Locate pattern matches with byte offsets}
\usage{
ac_locate_bytes(ac, doc, overlapping = FALSE, na = c("omit", "keep", "error"))
}
\arguments{
\item{ac}{An \verb{<ac_automaton>} object created by \code{ac_build()}.}

\item{doc}{A character vector of documents to search.}

\item{overlapping}{Default is \code{FALSE}. If \code{TRUE}, report overlapping
matches. This is only supported when \code{ac} was built with
\code{match_kind = "standard"}.}

\item{na}{How to handle \code{NA} documents. \code{"omit"} drops missing documents
(default); \code{"keep"} returns one row with missing result columns for each
missing document; \code{"error"} fails.}
}
\value{
A data frame with one row per match and four columns:
\code{doc_id}, \code{pattern_id}, \code{byte_start}, and \code{byte_end}.
}
\description{
\code{ac_locate_bytes()} searches a character vector with a compiled automaton
and returns byte offsets from the Rust \code{aho-corasick} crate. Byte offsets are
0-based, and \code{byte_end} is end-exclusive.
}
\examples{
ac <- ac_build(c("hello", "world"))
doc <- c("hello world", "nothing", "world hello")
ac_locate_bytes(ac, doc)
}
\seealso{
\code{\link[=ac_locate]{ac_locate()}}, \code{\link[=ac_locate_df]{ac_locate_df()}}.
}
