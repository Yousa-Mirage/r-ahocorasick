% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{ac_count}
\alias{ac_count}
\title{Count pattern matches in documents}
\usage{
ac_count(ac, doc, overlapping = FALSE, na = c("keep", "zero", "error"))
}
\arguments{
\item{ac}{An \verb{<ac_automaton>} object created by \code{ac_build()}.}

\item{doc}{A character vector of documents to search.}

\item{overlapping}{Default is \code{FALSE}. If \code{TRUE}, count overlapping
matches. This is only supported when \code{ac} was built with
\code{match_kind = "standard"}.}

\item{na}{How to handle \code{NA} documents. \code{"keep"} returns \code{NA_integer_}
(default); \code{"zero"} treats missing documents as zero matches; \code{"error"}
fails.}
}
\value{
An integer vector with the same length as \code{doc}.
}
\description{
\code{ac_count()} returns the number of pattern matches in each document.
}
\examples{
if (requireNamespace("dplyr", quietly = TRUE)) {
  ac <- ac_build(c("hello", "world"))
  docs <- data.frame(doc = c("hello world", "nothing", "world"))
  dplyr::mutate(docs, n_matches = ac_count(ac, doc))
}
}
\seealso{
\code{\link[=ac_count_file]{ac_count_file()}}, \code{\link[=ac_detect]{ac_detect()}}, \code{\link[=ac_locate]{ac_locate()}}, \code{\link[=ac_extract]{ac_extract()}}.
}
