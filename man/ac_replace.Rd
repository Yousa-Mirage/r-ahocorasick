% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace.R
\name{ac_replace}
\alias{ac_replace}
\title{Replace pattern matches in documents}
\usage{
ac_replace(ac, doc, replace_with, ..., na = c("keep", "empty", "error"))
}
\arguments{
\item{ac}{An \verb{<ac_automaton>} object created by \code{ac_build()}.}

\item{doc}{A character vector of documents to search and replace.}

\item{replace_with}{A character vector of replacements. If length 1, the
same replacement is used for every pattern. Otherwise, it \strong{MUST} have the
same length as \code{ac_patterns(ac)}, and replacements are matched to patterns
by position.}

\item{na}{How to handle \code{NA} documents. \code{"keep"} returns \code{NA_character_}
(default); \code{"empty"} treats missing documents as empty strings; \code{"error"}
fails.}
}
\value{
A character vector with the same length and names as \code{doc}.
}
\description{
\code{ac_replace()} replaces all non-overlapping matches in each document with
the corresponding replacement string.
}
\examples{
ac <- ac_build(c("fox", "brown", "quick"))
ac_replace(
  ac,
  "The quick brown fox.",
  c("sloth", "grey", "slow")
)

ac <- ac_build(c("append", "appendage", "app"), match_kind = "leftmost_first")
ac_replace(ac, "append the app to the appendage", c("x", "y", "z"))
}
\seealso{
\code{\link[=ac_build]{ac_build()}}, \code{\link[=ac_detect]{ac_detect()}}, \code{\link[=ac_count]{ac_count()}}, \code{\link[=ac_extract]{ac_extract()}},
\code{\link[=ac_locate]{ac_locate()}}.
}
